import React from "react";
import Form from "../components/Form";

const HomePage = () => {
  return (
    <>
      <Form title="Login" button="Login" isRegister={false} />
    </>
  );
};

export default HomePage;
